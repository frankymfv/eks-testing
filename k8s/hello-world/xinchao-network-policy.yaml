apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: xinchao-network-policy
  namespace: hello-world
spec:
  podSelector:
    matchLabels:
      app: xinchao-app-pod
  policyTypes:
  - Ingress
  - Egress
  ingress:
  # Allow traffic from user-app in user namespace
  - from:
    - namespaceSelector:
        matchLabels:
          name: user
      podSelector:
        matchLabels:
          app: user-app-pod
    ports:
    - protocol: TCP
      port: 8888
  # Allow traffic from ingress controller
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: ingress-nginx
    ports:
    - protocol: TCP
      port: 8888
  # Allow traffic from kube-system (for health checks)
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: kube-system
    ports:
    - protocol: TCP
      port: 8888
  egress:
  # Allow DNS resolution
  - to: []
    ports:
    - protocol: UDP
      port: 53
  # Allow access to kube-dns service
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: kube-system
      podSelector:
        matchLabels:
          k8s-app: kube-dns
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
